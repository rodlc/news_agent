<%# app/views/messages/_message.html.erb %>
<div
  class="mb-3 <%= message.direction == 'user' ? 'text-end' : 'text-start' %>"
  id="<%= dom_id(message) %>"
>
  <div
    class="d-inline-block p-3 rounded <%= message.direction == 'user' ? 'bg-primary text-white' : 'border' %>"
    style="max-width: 75%; <%= message.direction == 'assistant' ? 'background-color: #343a40;' : '' %>"
  >
    <%# Icône selon le rôle %>
    <% if message.direction == 'assistant' %>
      <strong>🤖 Assistant</strong>
    <% else %>
      <strong>👤 Toi</strong>
    <% end %>

    <hr class="my-2">

    <%# Contenu du message %>
    <%= simple_format(message.content) %>

    <%# Timestamp %>
    <small
      class="d-block mt-2"
      style="font-size: 0.75rem; <%= message.direction == 'user' ? 'color: rgba(255,255,255,0.7);' : 'color: #6c757d;' %>"
    >
      <%= message.created_at.strftime("%d/%m à %H:%M") %>
    </small>
  </div>
</div>
